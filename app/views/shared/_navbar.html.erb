
<div class="sticky top-0 z-10" data-controller="navbar" data-action="resize@window->navbar#closeOnBigScreen">
  <nav class="bg-slate-200 p-4 flex justify-between h-20 items-center">
    <div class="">logo</div>
    <div class="flex space-x-2 items-center">
      <div>email</div>
      <div class="md:hidden text-3xl" role="button" data-action="click->navbar#toggle">&#9776</div>
    </div>
  </nav>
  <nav class="bg-rose-200 absolute w-full h-80 overflow-y-auto hidden w-2/3 right-2" data-navbar-target="content">
    dropdown
    <% (300..350).each do |i| %>
      <p>Item <%= i %></p>
    <% end %>
  </nav>
</div>
